import { LoadingObserbablePipe } from './loading-obserbable.pipe';

describe('LoadingObserbablePipe', () => {
  it('create an instance', () => {
    const pipe = new LoadingObserbablePipe();
    expect(pipe).toBeTruthy();
  });
});
